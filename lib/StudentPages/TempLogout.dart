import 'package:flutter/material.dart';


class StuTempLogout extends StatelessWidget {
  const StuTempLogout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          children: [

            Text("Temporary Logout"),

          ],
        ),
      ),
    );
  }
}
